dependencies:
- express
- multer
- express-validator
- express-rate-limit
- google-libphonenumber
- is-base64
- helmet
- cors

setup: 
run `npm install`

create file: ".env" with 
"SERVER_BASE=`https://api-domain`"
"ORIGIN=`https://this-site-domain`"

run `server.js`